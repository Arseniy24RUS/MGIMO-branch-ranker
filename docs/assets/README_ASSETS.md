# Dashboard assets

The institutional logos `fnisc.png` and `mgimo-home.png` are included in this package and were taken from the user's public repository `Arseniy24RUS/Foreign-labor-migration/docs/assets/` as requested.

Leaflet and Plotly are vendored locally under `docs/assets/vendor/` for the production GitHub Pages build. `docs/index.html` loads those local copies for published pages.
